- name: Prepare the system
  hosts: all
  become: true
  tasks:
    - name: Install the required packages
      ansible.builtin.apt:
        name:
          - python3-pip
          - python3-virtualenv
        state: present
        update_cache: true
